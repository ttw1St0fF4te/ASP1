@model IEnumerable<Product>

@{
    ViewData["Title"] = "Каталог";
}

<link rel="stylesheet" href="~/animations/fade-in.css" asp-append-version="true"/>
<link rel="stylesheet" href="~/css/CatalogPage.css" asp-append-version="true"/>

<div class="fade-in">
    <div class="catalog-page">
        <div class="catalog-header">

            <!-- Элементы управления поиском и сортировкой -->
            <div class="catalog-controls">
                <form method="get" action="@Url.Action("Index", "Catalog")" id="catalogForm" class="search-container">
                    <button type="submit" class="search-btn">🔍</button>
                    <input type="text"
                           name="searchTerm"
                           value="@ViewBag.SearchTerm"
                           placeholder="Поиск товаров..."
                           class="search-input"
                           id="searchInput" />
                    <button type="button" class="clear-search" id="clearSearch">✕</button>
                    
                    <input type="hidden" name="sortBy" value="@ViewBag.SortBy" id="hiddenSortBy" />
                    <input type="hidden" name="sortOrder" value="@ViewBag.SortOrder" id="hiddenSortOrder" />
                </form>

                <div class="sort-container">
                    <select class="sort-select" id="sortSelect">
                        <option value="">По умолчанию</option>
                        <option value="price-asc" selected="@(ViewBag.SortBy == "price" && ViewBag.SortOrder == "asc")">По возрастанию цены</option>
                        <option value="price-desc" selected="@(ViewBag.SortBy == "price" && ViewBag.SortOrder == "desc")">По убыванию цены</option>
                        <option value="name-asc" selected="@(ViewBag.SortBy == "name" && ViewBag.SortOrder == "asc")">По названию А-Я</option>
                        <option value="name-desc" selected="@(ViewBag.SortBy == "name" && ViewBag.SortOrder == "desc")">По названию Я-А</option>
                        <option value="date-desc" selected="@(ViewBag.SortBy == "date" && ViewBag.SortOrder == "desc")">Сначала новые</option>
                        <option value="date-asc" selected="@(ViewBag.SortBy == "date" && ViewBag.SortOrder == "asc")">Сначала старые</option>
                    </select>
                </div>
            </div>
        </div>

        <!-- Результаты поиска -->
        @if (ViewBag.IsSearching && !ViewBag.HasResults)
        {
            <div class="no-results">
                <p>По вашему запросу ничего не найдено</p>
                <p>Попробуйте изменить параметры поиска</p>
            </div>
        }
        else
        {
            <div class="product-list">
                @foreach (var product in Model)
                {
                    <div class="product">
                        <div class="favorite-container">
                            @if (User.Identity.IsAuthenticated)
                            {
                                <form asp-controller="Favorite" asp-action="Toggle" method="post">
                                    <input type="hidden" name="productId" value="@product.Id" />
                                    <button type="submit" class="favorite-btn">
                                        ♡
                                    </button>
                                </form>
                            }
                        </div>
                        
                        <a asp-action="Details" asp-controller="Product" asp-route-id="@product.Id" class="product-link">
                            <img src="@product.Image" alt="@product.Name"/>
                        </a>
                        
                        <h3>@product.Name</h3>
                        <p class="product-price">@product.Price ₽</p>
                        <p class="product-category">@product.Category</p>
                        <p class="product-description">@product.Description</p>
                    </div>
                }
            </div>
        }
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const searchInput = document.getElementById('searchInput');
        const clearSearch = document.getElementById('clearSearch');
        const sortSelect = document.getElementById('sortSelect');
        const catalogForm = document.getElementById('catalogForm');
        const hiddenSortBy = document.getElementById('hiddenSortBy');
        const hiddenSortOrder = document.getElementById('hiddenSortOrder');
        const favoriteButtons = document.querySelectorAll('.favorite-btn');

        // Анимация при наведении на кнопку избранного
        favoriteButtons.forEach(button => {
            button.addEventListener('mouseover', function() {
                this.style.transform = 'scale(1.1)';
            });
            
            button.addEventListener('mouseout', function() {
                this.style.transform = 'scale(1)';
            });
            
            button.addEventListener('click', function() {
                this.classList.toggle('active');
                // Добавляем анимацию при клике
                this.style.transform = 'scale(1.2)';
                setTimeout(() => {
                    this.style.transform = 'scale(1)';
                }, 200);
            });
        });

        // Управление поиском
        function toggleClearButton() {
            if (searchInput.value.trim() !== '') {
                clearSearch.style.display = 'block';
            } else {
                clearSearch.style.display = 'none';
            }
        }

        // Инициализация видимости кнопки очистки
        toggleClearButton();

        // Обработчик ввода в поле поиска (только для показа/скрытия кнопки очистки)
        searchInput.addEventListener('input', function() {
            toggleClearButton();
        });

        // Обработчик кнопки очистки поиска
        clearSearch.addEventListener('click', function() {
            searchInput.value = '';
            toggleClearButton();
            catalogForm.submit();
        });

        // Обработчик изменения сортировки
        sortSelect.addEventListener('change', function() {
            const value = this.value;
            if (value === '') {
                hiddenSortBy.value = '';
                hiddenSortOrder.value = 'asc';
            } else {
                const parts = value.split('-');
                hiddenSortBy.value = parts[0];
                hiddenSortOrder.value = parts[1];
            }
            catalogForm.submit();
        });

        // Обработчик отправки формы (для поиска по Enter и по кнопке лупы)
        catalogForm.addEventListener('submit', function(e) {
            // Форма отправляется нормально, без preventDefault
        });
    });
</script>
